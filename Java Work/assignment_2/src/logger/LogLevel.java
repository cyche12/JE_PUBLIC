package logger;

/**
 * Enum for defining various log levels for the LMSLogger.
 * This can be expanded to include more log levels if needed.
 * @author Jake Elliott
 */
public enum LogLevel {
    INFO,
    ERROR,
    WARNING,
    DEBUG;
}
